class Login {
    constructor(userName, userId, token) {
        this.isLogin = false;
        this.userName = userName;
        this.userId = userId;
        this.token = token;
    }
}
export default Login